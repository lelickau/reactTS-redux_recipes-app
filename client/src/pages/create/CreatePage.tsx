import React, { FC } from 'react';

import './createPage.scss';

const CreatePage:FC = () => {
    return (
        <article className="create">
            
        </article>
    );
};

export default CreatePage;